# Core R script function, must be placed in the dependencies directory
##### Function of the script #####
# Use the igraph library to import and generate a relational matrix
# Run basic igraph library functions as shown in the generation 1.0 scripts
# Detach the igraph library and use the network/sna/statnet/coda package libraries
# Import user attribute data in another file, such as node id, gender, race, etc.
#use function for detach $detach("package:igraph",unload=TRUE)